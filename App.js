import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import MainContainer from './app/components/MainContainer/index'

export default class App extends React.Component {
  render() {
    return (
      <MainContainer />
    );
  }
}
